# Server-only: where the Next.js route handlers reach FastAPI (never exposed to the browser)
BACKEND_API_URL=http://localhost:8000
# Public canonical URL used for SEO metadata, sitemap and Open Graph (REPLACE before deployment)
NEXT_PUBLIC_SITE_URL=http://localhost:3000
